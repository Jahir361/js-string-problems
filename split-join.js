const detailse = 'hello iam hear to gain a best skill to get high pay job and feed my brain.No the actyal reson is i need to build my carrier.';
const parts = detailse.split(' ');
const sentancePart = detailse.split('.');
const charsParts = detailse.split('');
console.log(charsParts);


//slice 

const letsslice = detailse.slice(10,16);
console.log(letsslice);
const subStrings  = detailse.substring(10,16);
console.log(subStrings);